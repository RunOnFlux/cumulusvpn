import type { Catalog } from '../index';
import { en } from './en';

// UNTRANSLATED STUB — English copy. Replaced by its translation task
// (Tasks 7–9); the sentinel test in catalogs.test.ts fails on any leftover.
export const sv: Catalog = { ...en };
