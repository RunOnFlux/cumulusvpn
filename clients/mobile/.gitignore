node_modules/
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases

# iOS
ios/Pods/
ios/build/
ios/*.xcworkspace/xcuserdata/
*.hmap

# Android
android/.gradle/
android/app/build/
android/build/
android/local.properties
*.keystore
!debug.keystore

# Metro / RN
.metro-health-check*
*.log
.DS_Store

# wgnest multi-hop AAR + sources — build artifacts produced by
# clients/native/wgnest/build-android.sh (gomobile bind). Rebuilt in CI/locally.
android/app/libs/
