# Android store screenshots — REAL CAPTURES

Composed by `.claude/skills/playstore-screenshots` from **real device captures**
(Pixel 6 Pro, release build `installRelease` with local debug signing,
2026-07-21). Raws live in `../raw/android/`. All five storyboard frames
present, 1080×2160 RGB (Play-compliant, no alpha) — uploadable to Play Console.

Known blemish: a few status-bar notification icons remained visible despite
demo mode (clock 9:41 / wifi / battery were overridden). Re-capture on a
cleaner profile if pixel-perfect status bars are wanted.
