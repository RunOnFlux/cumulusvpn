# Standalone copy of the repo-root yarn config: the Docker build context is
# bridge/ alone, and node_modules linking is required for the native deps
# (better-sqlite3, secp256k1) and plain `node dist/` execution.
nodeLinker: node-modules
enableGlobalCache: true
